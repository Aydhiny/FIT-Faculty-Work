/*Napisati program u kojem je potrebno unijeti 
odabrani broj cifara počevši od cifre najveće težinske vrijednosti, 
pa od njih sastaviti i napisati prirodni broj. 
Unos cifara se prekida kada se unese broj manji od 0, ukoliko 
korisnik unese cifru veću od 9, ispisati poruku da cifra nije validna, 
ali se unos ne prekida.*/

#include <iostream>

using namespace std;

int main() {

	int c1;
	int c2 = 0;
	do {
		if (c1 >= 0, c1 <= 9)
		cout << "Unesite odredjenu cifru c1" << endl;
		cin >> c1;
		/
	} while (c1 >= 0);

		cout << "Unijeli ste vrijednost manju od 0!" << endl;
		return 0;


	return 0;
}